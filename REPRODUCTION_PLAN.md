# FFTCN Sleep-EDF-153 教学复现核心约束与进度总览

> 文档性质：**现行规范、项目总览与唯一执行入口**
> 最近核对：2026-07-28
> 历史快照和旧“下一步”均不具备执行效力；如与本文件冲突，以本文件为准。

## 1. 项目总览、当前进度与执行入口

### 1.1 整体目标与路线

- 当前目标：在 Sleep-EDF-153 上完成仓库代码版 FFTCN 的单数据集工程复现与教学重构。
- 合法结论：**“FFTCN 在 Sleep-EDF-153 上的单数据集工程复现。”**
- 当前非目标：SHHS-1/ISRUC-S1、论文三数据集完整复现、Sleep-EDF 十折严格复现、正式 HE/TEE 实现。
- 不得把代码混淆、源码加密或 NumPy 单文件部署描述为患者 EEG 隐私保护。

```text
0–1B  范围、环境、代码导览与原仓库结果级复现
  -> 2–9B  明文 PyTorch 教学重构闭环
  -> 9C     GitHub 项目化与 v0.1
  -> 10     NumPy 单文件推理
  -> 11     纯标准库重写（可选）
  -> 12     隐私改造准备
```

### 1.2 当前工作块与唯一下一步

```text
CURRENT_MILESTONE: 9C
CURRENT_INTERNAL_STEP: 3/3 GitHub fork、CI 与 v0.1 发布（整体验收完成）
CURRENT_STATUS: 已完成
ACTIVE_USER_STAGE: v0.1 已发布并通过整体验收；等待用户决定是否启动里程碑 10
CURRENT_STATUS_SOURCE: 本节 1.3 的 9C 行
FORMAL_ACCEPTANCE_UNIT: 整个里程碑 9C
```

- 9C 已固定为面向模型代码学习的 Sleep-EDF-153 教学复现仓库，不扩展为可安装
  Python 包，也不追加论文协议严格复现。
- 发布边界固定为：纳入源码、测试、运行脚本、学习指南、计划和小型 JSON 证据；
  排除数据、缓存、checkpoint、IDE/工具私有配置、生成的二进制结果、完整论文
  PDF/DOCX 及个人文献阅读/复现规划。
- 步骤一综合检查通过：暂存区共 114 个发布候选，禁发路径、意外根目录、二进制
  差异、误忽略发布材料、空白错误和疑似凭据均为 0；本地数据、checkpoint 与论文
  仍在磁盘，唯一未暂存项 `test01.py` 已归类为本机练习文件。
- 公开 fork 与 remote 角色已经配置：`origin` 指向用户公开 fork；`upstream`
  fetch 指向作者 `bjm-123/FFTCN`，push 为 `DISABLED`。首次发布 commit、`main`
  和 `v0.1` 指向同一已验收快照，commit 邮箱使用 GitHub `noreply`。
- 用户已明确委托 Codex 重写根 README；现稿已覆盖中英文定位、模型/数据契约、
  环境、短测试、数据与训练入口、9B 指标、差异、AI 辅助开发披露、归属和结论
  边界，本地链接与 69/69 无数据短回归通过。
- 已新增 `AI_AGENT_HANDOFF.md`：不同智能体必须从本计划、当前指南、源码和证据
  重建可核查上下文，不声称自动继承隐藏聊天记忆；动态进度仍只由本计划维护。
- `environment.yml` 已按验证版本建立；`README.pdf` 已从下一提交快照移除但仍保留
  在磁盘，`test01.py` 已写入当前克隆的 `.git/info/exclude`。`.gitignore`、
  `README.md`、`environment.yml` 和 `AI_AGENT_HANDOFF.md` 的最新内容均已进入
  暂存快照。
- 步骤二综合检查发现 4 项测试隐式读取本机 `datasets/`；已改为临时文件、mock EDF
  和内存标签夹具。修正后当前目录与匿名 ZIP tree 均为 69/69，三个 CLI、必需文件、
  README/AI 交接链接及发布边界全部通过。Windows `tar` 的中文路径乱码已改为
  ZIP + `Expand-Archive`。
- 当前材料：`learning_guides/milestone_09c/STEP_03_GITHUB_RELEASE.md`。
- `v0.1` 已建立正式 GitHub Release，未附加数据、checkpoint、论文或额外压缩包；
  CPU Actions、固定环境 69/69 短回归、三个 CLI 和干净发布快照均通过。
- 9C 整体验收确认：禁发路径与本机仓库绝对路径为 0；README、许可证、环境与
  9B 证据一致；四组正式实现、完整教程和独立参考接口同步，17 个教程 Python
  代码块可编译，既有人工注释质量门槛未被项目化修改破坏。
- 公开文档中的 Python 命令统一使用环境激活后的 `python`；项目文件与证据路径
  使用相对于仓库根目录的路径，不写入用户本机解释器、环境前缀或仓库绝对路径。

当前唯一下一步：里程碑 9C 已完成，当前不自动进入后续工作块。若用户决定继续，
必须先共同固定里程碑 10 的学习范围、内部步骤和整体验收条件；在此之前不得启动
NumPy 单文件推理改造、下载数据、训练或重复正式 test。

### 1.3 里程碑状态表

| 编号 | 里程碑 | 状态 | 主要证据 |
|---:|---|---|---|
| 0 | 计划与范围确认 | 已完成 | 本节 1.1 与第 4 节 |
| 1 | 环境与结构认知 | 已完成 | `reproduction_artifacts/milestone_01/` |
| 1A | 原仓库代码导览 | 已完成 | `reproduction_artifacts/code_walkthrough/` |
| 1B | 原仓库结果级直接复现预检 | 已完成 | `reproduction_artifacts/source_reproduction/` |
| 2 | Sleep-EDF 数据读取 | 已完成 | `reproduction_artifacts/milestone_02/` |
| 3 | 预处理与受试者级划分 | 已完成 | `reproduction_artifacts/milestone_03/` |
| 4 | Morlet CWT | 已完成 | `reproduction_artifacts/milestone_04/` |
| 5 | 1D-CNN 时域分支 | 已完成 | `reproduction_artifacts/milestone_05/` |
| 6 | 2D-CNN 时频分支 | 已完成 | `reproduction_artifacts/milestone_06/` |
| 7 | 特征融合与 TCN | 已完成 | `reproduction_artifacts/milestone_07/` |
| 8 | 两步训练核心接口 | 已完成 | `reproduction_artifacts/milestone_08/` |
| 9 | 工程评估接口 | 已完成 | `reproduction_artifacts/milestone_09/` |
| 9B | 教学重构模型完整训练验证 | 已完成 | `reproduction_artifacts/milestone_09b/validation_summary.json` 与正式报告 |
| **9C** | **GitHub 项目化与 v0.1** | **已完成** | **[v0.1 Release](https://github.com/Fatalove/FFTCN/releases/tag/v0.1)、[CPU Actions](https://github.com/Fatalove/FFTCN/actions/workflows/cpu-tests.yml) 与 `reproduction_artifacts/MILESTONE_LOG.md`** |
| 10 | NumPy 单文件推理 | 未开始 | 9C 后的独立扩展 |
| 11 | 纯标准库重写 | 未开始 | 可选，不阻塞 v0.1 |
| 12 | 隐私改造准备 | 未开始 | 仅做威胁模型与适配边界 |

## 2. 当前里程碑 9C 边界

### 2.1 目标与学习边界

把 2–9B 已验收的教学重构整理为公开、可复核的 GitHub fork 和 `v0.1`。主要学习
Git 的工作区/暂存区/提交、ignore、remote、分支、tag、CI 和 release，而不是把
项目改造成 PyPI 包。9B 的正式结果直接作为发布输入，不重新训练或重复 test。

### 2.2 三个内部步骤

| 步骤 | 用户练习核心 | Codex 辅助材料 |
|---:|---|---|
| 1 | 理解工作区/暂存区/HEAD 和 `AM/AD/??`；补全 `.gitignore`；随后按意图重建暂存集 | 只读盘点、发布分类、命令解释和行为检查 |
| 2 | 共同完成面向学习者的 README、环境文件和最小复现入口 | 文档骨架、事实核对、短回归与干净克隆检查 |
| 3 | 理解 fork、`origin/upstream`、commit、CI、tag 和 release；亲手执行关键 Git 操作 | 精确命令、远程目标复核、CI/发布检查 |

每个步骤开始时一次性交付覆盖完整操作链和最终自测的指导文件，用户完成整步后
只申请一次综合检查；中途不设置必须停下交回的检查点。步骤综合检查不等于正式
验收，正式状态和最终验收仍只属于整个 9C。

### 2.3 固定发布边界

1. 纳入：`data/`、`models/`、`training/`、`evaluation/`、`scripts/`、`tests/`、
   `learning_guides/`、计划、许可证、环境文件和经过筛选的小型 Markdown/JSON 证据。
2. 排除：`datasets/` 实体、训练缓存、`*.pt/*.pth`、IDE/个人工具配置、
   `source_reproduction/` 生成输出、`*.npz/*.xlsx` 结果和完整论文 PDF/DOCX。
3. 正式报告 JSON 可以发布；正式 checkpoint 不进入 Git 历史或 v0.1 release。
4. 保留上游 MIT 许可证与作者版权，并在 README 明确 fork、教学修改和结论边界。
5. README 以中文为主并带简短英文概述；必须写明当前 `d21` 环境、Sleep-EDF
   获取方式、短测试、训练入口、9B 指标和论文/发布代码差异。
6. 公开命令先要求用户按 `environment.yml` 建立并激活环境，再使用 `python`；
   项目文件和证据优先采用相对于仓库根目录的路径，不固化用户本机安装位置。

### 2.4 整个 9C 的统一通过条件

1. 暂存快照只含发布范围内文件，不含数据、checkpoint、IDE、完整论文或生成二进制。
2. `upstream` 指向作者仓库，`origin` 指向用户公开 fork，不向作者远程 push。
3. README、环境文件、许可证/归属、数据获取、测试、训练、结果与结论边界完整准确。
4. 固定环境下全部短回归通过；GitHub Actions 的最小 CPU 检查通过。
5. 从干净克隆按 README 能建立环境、运行短检查并定位完整训练入口。
6. 公开 fork 的已验收提交具有 `v0.1` tag 和 GitHub Release；记录 commit SHA 与 URL。
7. 正式验收仍检查三份教学接口同步、注释质量和 9B 证据链接没有被项目化破坏。

### 2.5 外部动作边界

步骤一和步骤二只做本地、可恢复的文件与验证工作。创建 fork、改变 remote、push、
tag 和 GitHub Release 属于步骤三外部动作；到达该步骤时必须再次展示精确仓库、
分支、提交和版本目标，获得用户明确确认后才能执行。

## 3. 术语与不可违反的执行规则

### 3.1 术语

| 术语 | 含义 |
|---|---|
| 里程碑 | 唯一可以拥有正式状态、接受整体检查并被标记完成的工作单元 |
| 内部步骤 | 同一里程碑内的教学和依赖顺序；只能自测，不能单独验收 |
| 函数 | 编码实现单位，不是检查或状态单位 |
| 行为自测 | 为尽早定位错误而运行的局部测试，不改变里程碑状态 |
| 步骤综合检查 | 用户完成一个内部步骤后进行的一次集中检查；用于决定是否进入下一内部步骤，不改变里程碑正式状态 |
| 正式验收 | 对整个里程碑的代码、测试、回归、文档、实验事实和通过条件统一检查 |

### 3.2 里程碑、状态与不确定性边界

1. 一次只推进状态表中第一个未完成的里程碑，不跨里程碑生成或实现后续内容。
2. 内部步骤可按运行链准备材料和自测，但不得编号成子里程碑或使用正式状态。
3. 每个内部步骤的指导文件必须在步骤开始时一次性给全，覆盖问题分析、必要基础、
   手工推演、全部用户操作、最终自测、预期结果与禁止动作；不得把同一步拆成
   “做一小项—交回检查—再发下一小项”的串行检查。
4. 用户完成一个内部步骤后只申请一次步骤综合检查；具体检查项由 Codex 根据该步
   契约统一决定。除非出现会改变范围、实验语义、验收标准或产生破坏性影响的真实
   阻塞，中途提问和过程反馈都不得被升级为必须停下的检查点。
5. 不要求用户写完一个函数就停下来申请检查；函数级和步骤级测试只是用户自测，
   Codex 在步骤综合检查时集中核对。
6. 只有用户明确表示**整个当前里程碑**完成并申请检查，才进入正式验收；通过后才能标记完成并进入下一里程碑。
7. 用户中途提问、请求解释、提交局部代码或要求诊断，不等于申请里程碑验收。
8. 收到“已完成”“请检查”时，先结合第 1.2 节当前工作块和用户明确用词判断范围；只有读取进度后仍存在真实歧义时才询问，不得机械追问。
9. 新对话、上下文压缩或任务恢复后，必须先完整读取本文件，尤其是第 1.2 节当前工作块和第 1.3 节状态表，再按其中路径核对当前产物；不得用旧会话记忆猜测进度，也不得在文件已能确定答案时把进度问题反问用户。
10. 修改方向能由现有规则、文件和测试确定时，同一轮完成分析、修改和适度验证；仅在真实未决选择会改变范围、实验语义、验收标准、大量或破坏性数据写入、外部动作或长时间运行时暂停讨论。
11. 允许状态为 `未开始`、`进行中`、`等待用户练习`、`等待里程碑验收`、`已完成`、`受阻`。内部步骤和函数不使用这些正式状态。
12. `等待用户练习`表示用户正式实现、实验或说明尚未完成；`等待里程碑验收`表示用户已明确声明整个里程碑完成、尚待统一检查；`已完成`只在整个里程碑全部通过条件与回归满足后使用。不得因单个测试或内部步骤改变正式状态。
### 3.3 学习目标优先级与引导式编码协议

1. 首要目标是让用户理解并亲手实现当前里程碑的核心代码与完整数据流，而不是堆出功能最多的工程。模型阶段重点学习结构、层参数、张量语义和前向/推理；训练阶段重点学习损失、梯度、优化器、状态切换、checkpoint 和评估；数据阶段重点学习时间对齐、标签、记录/序列边界和模型输入契约。
2. 决定样本含义、模型数学、训练结果或评估可信度的逻辑都属于核心；CLI、部署便利和面向未知输入的全面防御属于辅助工程或选读加固。
3. 每个内部步骤先明确：用户练习核心、Codex 辅助材料、工程加固（选读）。
4. 教学顺序固定为“问题分析 → 必要 Python/NumPy/PyTorch 基础 → 手工推演 → 带详细注释的完整核心代码 → 用户编码 → 行为自测”。
5. Codex 可准备最小骨架、公开接口、形状/类型契约、伪代码、行为测试和独立参考，但不得替用户完成标记为练习的正式核心实现。
6. 正式练习不得导入参考答案；Codex 不得用参考答案覆盖用户代码。
7. 正式骨架、教程完整代码和参考答案的公开接口与调用关系必须同步；参考只覆盖当前内部步骤。
8. 主参考代码保持最小、准确并突出核心数据流；只有能防止固定运行链发生静默语义错误的检查才进入主代码，例如轴/形状广播、样本/标签错位、跨记录、数据泄漏、无效数值运算或 checkpoint 不兼容。每项核心检查都必须说明具体失败方式。
9. 假想恶意输入、流程不会产生的边界、重复验证可信结果或只改善报错体验的检查默认移入“工程加固（选读）”，不得据此扩大用户练习或正式验收。不确定检查是否必要时，先分析可观察失败，再与用户讨论，不能用“最佳实践”替代证据。
10. 说明、docstring 和注释默认中文，函数名、变量名和 API 遵循代码规范；核心注释解释做什么、为什么、输入输出和形状/类型如何变化，不能只复述语法。
11. 张量变形或轴转换先解释模型意图和轴语义，再解释框架接口；不得把特征维误称为 EEG 物理通道。
12. 教学材料交付前必须人工通过 `learning_guides/COMMENT_QUALITY_GATE.md`；语法、测试数量或注释行数不能替代人工审读。
13. 必须由用户选择时，不得只罗列选项或只问“选哪个”；应同时说明各选项的实际
    影响、风险/代价、推荐项及推荐理由，并给出选定后的准确操作。

### 3.4 测试与整体验收

1. 测试优先验证可观察行为、公开接口、形状、算法语义、梯度、复现性和实验边界。
2. 不强制无语义价值的内部命名、参数书写方式或风格；只有影响公开 API、算法、形状、随机性、`state_dict` 或 checkpoint 兼容性时才可阻塞。
3. 正式验收检查当前里程碑全部通过条件、聚焦测试、必要前序回归、三份接口同步和注释质量。
4. 参考答案通过只能证明教学材料正确，不能算用户实现进度。
5. 正式代码仍含 `NotImplementedError`、隐式空返回或未完成分支时，不得描述为可用。
6. 新增阻塞测试必须对应公开契约、已确认算法语义或可复现静默错误，不能来自风格偏好或假想生产风险。
7. “工程加固（选读）”默认不进入正式条件，除非用户明确提升范围或固定运行链出现具体故障。

## 4. 固定技术契约

### 4.1 资料优先级与实现基准

1. `背景/01 基于时序卷积网络的单通道EEG自动睡眠分期特征融合模型_中文译文.docx`：论文文字首要来源；
2. `背景/01 A Feature Fusion Model Based on Temporal Convolutional Network for Automatic Sleep Staging Using Single-Channel EEG.pdf`：核对公式、图表、页码、版式和译文疑点；
3. 本机私有的 `背景/deep-research-report.md`：核对学习路线、从零实现和隐私方向，
   仅服务当前学习过程，不随公开仓库发布；
4. 当前仓库源码：确认本项目实际执行行为。

核对论文文字时优先检索中文译本；原 PDF 是图片型，只在核对公式、图表、页码、版式或译文歧义时渲染，不得因原 PDF 缺少文本层而跳过可直接检索的中文译本。论文与仓库不一致时记录差异并默认实现仓库行为。固定差异包括：CWT `[1,30,60]`、时域/小波特征 `256/216`、融合 `472`、TCN dilation `1/2/4/8`。

### 4.2 数据契约

| 项目 | 固定契约 |
|---|---|
| 数据集 | Sleep-EDF Expanded v1.0.0 sleep-cassette，153 records / 78 subjects |
| 预处理根目录 | `datasets/sleep-edf-153-processed-v1/` |
| 固定划分 | seed 42；train/validation/test 为 62/8/8 subjects，集合两两无交集 |
| 已处理数量 | train `154,128`，validation `23,315`，test `18,036`，合计 `195,479` epochs |
| 记录文件 | `record_id.npz`；`x:[N,1,3000] float32`，`y:[N] int64` |
| 标签 | W=0、N1=1、N2=2、N3=3、REM=4；S3/S4→N3；删除 Movement/Unknown |
| 缓存链 | record NPZ → `raw.npy`/`labels.npy`/`manifest.json` → 只调用 `data/sleep_edf_cwt.py` 重构对齐的 `wave.npy` |
| 序列边界 | 清洗后紧凑排列；T=50 不跨记录；每条记录不足 50 的尾部分别丢弃 |

### 4.3 模型与形状契约

| 对象 | 形状/数值 |
|---|---|
| 单个/连续 raw | `[1,3000]` / `[T,1,3000]` |
| 单个/连续 wave | `[1,30,60]` / `[T,1,30,60]` |
| 时域/小波特征 | `[T,256]` / `[T,216]` |
| 融合特征 | `[T,472]`；472 是每个 epoch 的融合特征数，不是 EEG 通道数 |
| TCN 表征 / logits | `[T,128]` / `[T,5]` |
| 训练序列 | 融合 `T=50`；分支预训练等价于 `T=1` |
| TCN | 非因果 same padding；dilation `1/2/4/8` |

### 4.4 当前运行环境

- Python：`d21` 环境，Python 3.10.13。
- PyTorch 1.12.1 + CUDA 11.6；NumPy 1.22.3；SciPy 1.10.1。
- GPU：RTX 3050 Laptop 4 GB；长训练或全量 CWT 前必须先完成显存、耗时和恢复预检。用户入口在当前终端显示长阶段进度，`scripts/run_milestone_09b.py status` 只用于可选旁路检查。
- 当前 `d21` PowerShell 已能直接调用 `git`；指南使用短命令。若未来新终端无法识别，
  再回退到 Codex bundled Git 的完整路径，不修改系统级 Git 配置。

## 5. 实验事实、代码变更与结论边界

1. 必须区分论文结果、原仓库直接复现结果、教学重构接口/冒烟验证、教学重构模型完整训练结果。
2. “代码可运行”“单 batch 可更新”“测试通过”均不等于完整训练、收敛或论文指标复现。
3. 里程碑 1B 的 `Accuracy 0.7917977528`、`Macro-F1 0.7010606829`、`kappa 0.7141909029` 来自原仓库完整训练 checkpoint。
4. 里程碑 9 复用同一 1B checkpoint 和等价预测，只验证教学评估接口，没有产生第二个训练模型。
5. 里程碑 9B 已完成整体验收：固定 seed 0 的 raw/wave/fusion 为 `20/20/50` 轮；
   fusion validation-best 在 `17,800` 个 test 位置上正式评估一次，Accuracy
   `0.7941573034`、Macro-F1 `0.7055190242`、kappa `0.7169520827`。这是
   Sleep-EDF-153 单数据集、单种子工程结果，不是论文十折或多种子稳定性结论。
6. test 有 `18,036` 个处理后 epoch；按记录组成 T=50 并丢弃各记录尾部后为 `17,800` 个评估位置，二者不得混称。
7. test 不参与模型选择；best 只能由 validation 确定，test 只在模型选择结束后正式评估。
8. 相同配置重训不要求指标逐位一致；单 seed 不能表述为多 seed 稳定性。
9. 代码变更在对应产物或 `reproduction_artifacts/MILESTONE_LOG.md` 记录修改位置、问题、理由、语义影响和验证；本文件不追加调试流水账。
10. 原仓库直接复现只允许改路径、配置、数据接口、设备、checkpoint 和指标等边界层，不静默改变模型、损失或训练语义。

## 6. 文档地图与维护规则

### 6.1 文档权限

1. 本文件：唯一现行规范、项目状态和执行入口。
2. `learning_guides/`：详细教学材料，不定义跨里程碑状态。
3. `tests/`：可执行行为契约；测试通过不自动改变正式状态。
4. `reproduction_artifacts/milestone_XX/`：验证事实和产物证据。
5. `reproduction_artifacts/MILESTONE_LOG.md`：可复核节点日志。
6. `reproduction_artifacts/plan_history/`：非规范历史；旧状态、旧验收和旧“下一步”不得作为当前指令。

### 6.2 主文件维护

1. 不向本文件追加实验日志、调试过程、旧失败命令或已完成里程碑的完整教程。
2. 当前动作与唯一下一步只维护在 1.2；文末不得建立第二个入口。
3. 正式状态只维护在 1.3；状态变化时同步 1.2，只有契约事实变化时才更新第 2 节。
4. 已完成里程碑只保留状态和证据链接；未开始里程碑只保留路线图目标。
5. 主文件原则上保持在 300 行以内；接近上限时先迁出非规范说明。
6. 修改后核对：1.2 与 1.3 一致、链接存在、无历史命令式“下一步”、无步骤级正式验收措辞。
7. 重构时先区分长期规则、当前状态、固定契约、历史证据和过期指令；长期教学规则不得随旧日志一并删除。
8. 每次规则重构至少复核：中文译本优先、核心学习目标、用户亲写核心代码、最小正确性检查、行为优先测试、里程碑整体验收。

继续项目的标准请求：

> 请读取 `REPRODUCTION_PLAN.md`，只推进当前工作块指定的里程碑，并遵守里程碑整体验收与引导式编码协议。不清楚的情况直接与我讨论，不要随意猜测。
