# FFTCN 智能体项目交接协议

> 版本：2026-07-27
> 适用对象：Codex、Claude 及其他能够读取本地仓库文件的编程智能体

## 1. 文件目的

本文件用于让新的智能体会话可靠恢复 FFTCN 教学复现项目的协作方式。它不是聊天
记录，也不能让一个智能体继承另一个智能体的隐藏记忆。正确做法是读取现行规范、
源码、测试和证据，重新建立可核查的上下文。

不要在本文件复制会变化的当前步骤、下一条命令或训练进度。动态状态只由
`REPRODUCTION_PLAN.md` 的“当前工作块与唯一下一步”决定。

## 2. 信息权威顺序

遇到不一致时按以下顺序处理：

1. `REPRODUCTION_PLAN.md`：唯一现行范围、状态、当前工作块和验收协议；
2. 计划为当前步骤指定的 `learning_guides/` 指导文件；
3. 正式源码及其行为测试；
4. `reproduction_artifacts/` 中的正式报告、验证摘要和 `MILESTONE_LOG.md`；
5. `reproduction_artifacts/plan_history/`：只用于追溯，不提供当前命令。

论文和上游仓库用于解释来源与差异。若论文描述、上游发布代码和本教学重构不一致，
应先记录差异，再遵循现行计划指定的项目语义，不得自行把目标改成“论文协议严格
复现”。

## 3. 新会话启动协议

新的智能体在修改文件或给出下一步之前，必须：

1. 完整读取本文件和 `REPRODUCTION_PLAN.md`；
2. 从计划中提取 `CURRENT_MILESTONE`、`CURRENT_INTERNAL_STEP`、
   `CURRENT_STATUS`、`ACTIVE_USER_STAGE` 和 `FORMAL_ACCEPTANCE_UNIT`；
3. 完整读取计划指定的当前指导文件，并检查其直接引用的代码、测试和证据；
4. 用实际 `git status` / `git diff` 核对工作区，不把旧聊天摘要当作当前事实；
5. 先向用户简要复述当前工作块、允许动作、禁止动作和唯一下一步，再开始工作；
6. 只推进当前工作块，不预做后续里程碑。

若现行文件足以确定答案，直接按其执行。只有当不确定性会改变项目范围、实验语义、
验收标准、破坏性操作、外部发布或长时间运行时，才停下来与用户讨论，不得猜测。

## 4. 教学与验收协议

- 里程碑是唯一拥有正式状态和整体验收的单位；内部步骤只用于模块化教学。
- 每个内部步骤的指导文件应一次性给全：问题分析、必要基础、手工推演、全部操作、
  最终自测、预期结果和禁止动作。
- 用户完成整个内部步骤后只做一次综合检查；不得把每个子命令拆成频繁交回点。
- 正式验收只在整个里程碑完成时进行，且必须同时覆盖代码、测试、文档和证据。
- 用户负责练习中标记为核心的数据、模型与训练实现；智能体可以准备接口、伪代码、
  完整独立参考、行为测试、教学文档和错误解释。用户明确委托的文档或确定性辅助
  修改可由智能体直接完成。
- 解释张量变换时先说明模型意图和每个轴的语义，再解释框架 API；融合特征维不等于
  EEG 物理通道数。
- 测试以公开接口、形状、数值、梯度、可复现性和 checkpoint 兼容等可观察行为为
  契约。除非影响这些契约，不强制内部变量或层属性改名。
- 主路径保留最小准确实现；额外防御性代码放入“工程加固（选读）”。

## 5. 运行与发布安全边界

- 未经用户明确授权，不下载完整数据、不构建全量缓存、不更新真实模型，也不启动
  长时间训练。
- 长训练由用户在发起终端启动。训练进度以真实 batch 为单位，显示累计位置加权
  `loss` 和 `acc`；不得用后台轮询伪装实时进度。
- 正式 test 只在模型选择结束后执行约定次数，不为观察指标而重复运行。
- 保留用户现有工作区改动；不使用破坏性 Git 命令清空未知内容。
- 创建 fork、改变 remote、commit、push、tag、release 等动作，必须符合当前步骤
  并在执行前核对精确仓库、分支、提交和版本目标。
- 数据、缓存、checkpoint、个人 IDE/智能体配置、生成二进制和完整论文不进入公开
  源码历史；正式小型 JSON/Markdown 证据可以按计划发布。

## 6. 稳定项目事实

- 项目目标是学习和重构实际可运行的 FFTCN 模型代码，不是论文协议严格复现。
- 当前数据范围仅为 Sleep-EDF Expanded v1.0.0 的 sleep-cassette 子集：
  153 条记录、78 名受试者。
- 固定受试者划分使用 seed 42，train/validation/test 为 62/8/8，集合互不重叠。
- 正式融合序列长度为 `T=50`，序列不得跨记录。
- 每个 epoch 的时域/时频/融合特征维为 256/216/472；472 是特征数，不是 EEG
  通道数。
- TCN 使用 dilation `1/2/4/8`，输出 5 类睡眠阶段。
- 已记录的教学重构正式运行是 seed 0、raw/wave/fusion `20/20/50` 轮；
  固定 test 的 17,800 个位置上，Accuracy 为 `0.7941573034`、Macro-F1 为
  `0.7055190242`、Cohen kappa 为 `0.7169520827`。
- 这些指标是单数据集、单种子工程证据，不是论文十折均值或多种子稳定性结论。

所有可能变化的阶段状态和下一步仍须从 `REPRODUCTION_PLAN.md` 现场读取。

## 7. AI 辅助与责任边界

OpenAI Codex 在本项目中用于计划维护、概念讲解、教学指南与文档、测试辅助、
调试建议、确定性工程修正，以及用户运行后产物的检查。用户负责范围和验收决策，
完成或审核核心实现，显式启动长实验，并对最终代码、结果、引用和表述负责。

智能体不是作者或最终责任主体，其生成文字也不是独立实验依据。不要隐藏其对项目
的实质性参与，也不要未经核验直接采纳其输出。若项目进入课程、论文或其他正式
提交，应按对应机构另行完成 AI 使用披露；仓库声明不能代替提交方规则。

## 8. 可直接交给新智能体的提示

```text
请先完整读取 AI_AGENT_HANDOFF.md 和 REPRODUCTION_PLAN.md，再读取计划指定的
当前指导文件与直接引用证据。请先复述当前里程碑、内部步骤、允许/禁止动作和
唯一下一步；之后只推进当前工作块，并遵守里程碑整体验收与引导式编码协议。
若现行文件无法确定且不同选择会改变范围、实验语义、验收、外部发布或长时间
运行，请直接与我讨论，不要猜测。
```

## 9. 维护规则

- 只在协作协议或稳定项目事实改变时更新本文件；
- 进度变化只更新 `REPRODUCTION_PLAN.md`，完成事件写入 `MILESTONE_LOG.md`；
- 不粘贴完整聊天记录、密钥、个人绝对路径、数据或 checkpoint；
- 新增面向特定智能体的入口文件时，应链接回本文件，避免维护多份冲突协议。
